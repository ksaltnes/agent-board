{
  "name": "agent-board",
  "version": "1.0.0",
  "description": "Minimal multi-agent project tracker — compact context, low-token ticket ops",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "seed": "node src/seed.js",
    "test": "node src/smoke-test.js && node --test src/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
